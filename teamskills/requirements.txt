requests
python-dotenv
pdfplumber
python-dateutil
# optional (useful for image OCR and Vision fallback)
# Local OCR (Tesseract): uncomment if you have system tesseract installed
# pytesseract
# PDF -> image conversion (needed by Vision OCR flow)
pdf2image
# Image handling
pillow
# Google Cloud Vision client (optional; requires service account credentials)
google-cloud-vision
 
